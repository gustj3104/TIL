package final_assign.result;


/**
 * undirected, unweighted graph에서 인접리스트 구현
 * 각 노드마다 연결된 노드 구하기
 */
public class AdjacencyList1 {

    // 각 노드 내에 저장될 요소 수는 알 수 없음 -> ArrayList
        // 각 ArrayList를 정점의 개수만큼 할당

    // graph[u] 내부는 ArrayList로 u와 연결된 노드들이 저장
    // graph[u] = v, graph[v] = u
        // u - v가 각각 추가됨

}

